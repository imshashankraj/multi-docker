sudo: required
services:
  - docker

before_install:
  - docker build -t shashank/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run shashank/react-test npm test -- --coverage

after_success:
  - docker build -t imshashankraj/multi-client ./client
  - docker build -t imshashankraj/multi-nginx ./nginx
  - docker build -t imshashankraj/multi-server ./server
  - docker build -t imshashankraj/multi-worker ./worker
  # Log into the Docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  ## Take those images and push them to docker hub
  - docker push imshashankraj/multi-client
  - docker push imshashankraj/multi-nginx
  - docker push imshashankraj/multi-server
  - docker push imshashankraj/multi-worker

